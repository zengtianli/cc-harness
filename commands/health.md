运行 ZDWP 工作目录健康检查。

执行步骤：
1. 运行 `python3 $ARGUMENTS scripts/zdwp_health.py` （如果用户说 --llm 则加上该参数）
2. 分析输出结果，对每个错误和警告给出修复建议
3. 如果有 Git 数据泄漏，建议更新 .gitignore 并 git rm --cached
4. 如果有版本冗余文件，建议保留最新版并归档旧版
5. 如果有散乱文件，建议移到对应项目目录

注意：只报告问题和建议，不要自动修复，等用户确认后再操作。
